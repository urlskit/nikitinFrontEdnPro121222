const firstNumber = parseInt(prompt('Введите первое число'));
const secondNumber = parseInt(prompt('Введите второе число'));
const thirdNumber = parseInt(prompt('Введите третье число'));
const sAr = 3;

const sum = (firstNumber + secondNumber + thirdNumber) / sAr;

alert(`Cреднее арифметическое = ${sum}`);